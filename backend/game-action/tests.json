{
  "tests": [
    {
      "name": "Без session_id — ошибка",
      "method": "GET",
      "path": "/?action=state",
      "expectedStatus": 400,
      "expectedBody": {"error": "session_id required"},
      "bodyMatcher": "partial"
    }
  ]
}
